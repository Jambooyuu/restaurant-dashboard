# AI_USAGE.md — AI 工具使用说明

## 使用的 AI 工具

本项目开发过程中使用了 **DeepSeek Harness (MiMo-v2.5-pro)** 作为编程助手。

---

## 任务拆解方式

将 24 小时作业拆解为以下阶段，每个阶段用一个独立的 prompt 推进：

### Prompt 示例（数据清洗阶段）

```
你是我的资深全栈工程搭档，一起完成一个 24 小时限时的求职实操作业。
项目:连锁餐饮(5 门店)数据分析看板 + AI 自然语言数据问答。
技术栈:Python FastAPI + SQLite + React(Vite) + ECharts,AI 用 DeepSeek API。

三条铁律:
1. 数字只来自数据库真实查询
2. 分阶段交付，每阶段动手前先给我看关键决策
3. 按开发过程多次 commit

第一阶段:读取三张 CSV 样本，统计脏数据情况，给出清洗规则表，清洗后核对再入库。
```

这种 prompt 的优点：
- **明确角色**：资深全栈搭档，不是通用助手
- **铁律约束**：防止 AI 编数字、跳步骤、一次性提交
- **分阶段**：每次只做一件事，做完确认再进下一步

---

## AI 在哪里出了错

### 1. 脏数据扫描时遗漏了 ¥ 前缀

**发现过程：** 第一版扫描脚本直接 `float(amount)` 遇到 `¥66.00` 报 ValueError，说明 AI 初始生成的扫描脚本没有考虑货币符号混入的情况。

**修复：** 加入 `amount.replace('¥', '').replace('\xa5', '')` 清洗逻辑。

### 2. 负金额的处理策略

**初始想法：** AI 建议直接删除负金额行。

**修正：** 负金额在餐饮 POS 中通常是退款/退货，应该保留在数据库中（保持完整流水），在统计时自然纳入（退款拉低营业额）。这个判断需要业务理解，AI 初始方案过于简单。

### 3. 空 amount 的回填逻辑

**发现：** 120 行 amount 为空，但 qty 和 product_id 有值。

**AI 方案：** 用 `qty × unit_price` 回填。这个方案合理，因为产品表单价是可信的主数据。

---

## 哪些部分我自己写

### 1. 清洗规则的最终拍板

AI 提供了多种策略选项（删除 vs 保留 vs 回填），但最终决策需要业务判断：
- 负金额保留（退款是真实业务）
- 空 amount 回填（单价可信）
- S99/P99 删除（无法关联维度）

这些决策我独立做出，AI 只提供数据支撑。

### 2. 防幻觉方案设计

AI 问答的 function calling 架构是我设计的：
- AI 不能直接访问数据库
- 唯一数据出口是 `query_sales_data()` 函数
- 每个数字都能追溯到 SQL 执行结果

这个架构确保了"数字来自数据库"的铁律，而不是让 AI 自己编。

### 3. 前端信息层级

看板的 KPI 卡片 → 趋势图 → 门店对比 → 品类/支付 → Top10 → AI 对话的信息层级是我排的，不是套组件库模板。

---

## 总结

| 环节 | AI 参与度 | 我的参与 |
|------|----------|---------|
| 脏数据扫描 | 90%（生成脚本） | 10%（发现遗漏、补充规则） |
| 清洗策略 | 50%（提供建议） | 50%（业务判断拍板） |
| 后端 API | 85%（生成代码） | 15%（review、调整接口设计） |
| 前端组件 | 80%（生成代码） | 20%（信息层级、样式微调） |
| AI 问答架构 | 30%（实现细节） | 70%（架构设计、防幻觉方案） |
| 文档 | 70%（生成初稿） | 30%（补充业务细节） |
